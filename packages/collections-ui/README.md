# collections-ui (planned, TypeScript)

Fully generic web frontend. No collection-specific pages: table, search, detail,
create and edit views are all generated from JSON Schema. Talks only to the
generic REST API and adapts to the reported capabilities (e.g. hides write actions
on a read-only deployment).

This is the one TypeScript package in the monorepo. Not implemented in milestone 1.
