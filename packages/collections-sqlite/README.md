# collections-sqlite (planned)

A SQLite `StorageProvider` with full CRUD and transaction support. Because the
REST API and UI depend only on `collections-core` interfaces and capabilities,
switching from the filesystem provider to SQLite requires **no** changes to the
API or UI. Not implemented in milestone 1.
