# Fly.io deployment for the remote MCP server. Rename `app` and set secrets before
# deploying (see packages/collections-mcp/README.md).
app = "collections-mcp"
primary_region = "fra"

[build]

[env]
  # COLLECTIONS_MCP_TOKEN (the bearer secret) is set via `fly secrets set`.
  # For ChatGPT/Claude.ai custom connectors (which require a real OAuth flow, not
  # a bare token), also set COLLECTIONS_MCP_OAUTH_CLIENT_ID/_CLIENT_SECRET/
  # _REDIRECT_URIS and COLLECTIONS_MCP_PUBLIC_URL — see
  # packages/collections-mcp/README.md.

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

# Durable storage for the JSON collections; mounted at /data, used as /data/collections.
[[mounts]]
  source = "collections_data"
  destination = "/data"
